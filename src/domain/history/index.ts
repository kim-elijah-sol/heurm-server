import { getHistory } from './history.get';
import { patchHistory } from './history.patch';
import { postHistory } from './history.post';

export const history = {
  postHistory,
  patchHistory,
  getHistory,
};
